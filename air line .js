const popup = document.getElementById("sign-in-popup");
const signInButton = document.getElementById("sign-in-button"); // Replace with your button ID
function togglePopup() {
    popup.classList.toggle("hidden"); // Toggles "hidden" class
  }
  signInButton.addEventListener("click", togglePopup);
  const popup = document.getElementById("sign-in-popup");
const signInButton = document.getElementById("sign-in-button");

function togglePopup() {
  popup.classList.toggle("hidden");
}

signInButton.addEventListener("click", togglePopup);
const popup = document.getElementById("sign-in-popup");
const signInButton = document.getElementById("sign-in-button");

function togglePopup() {
  popup.classList.toggle("hidden");
}

signInButton.addEventListener("click", togglePopup);